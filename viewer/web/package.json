{
  "name": "nebuladisplay-web-viewer",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "description": "NebulaDisplay browser viewer + control panel (NDSP over WebSocket, WebCodecs H.264 / JPEG)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "playwright": "^1.61.1",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@noble/ciphers": "^2.2.0",
    "@noble/curves": "^2.2.0",
    "@noble/hashes": "^2.2.0"
  }
}
